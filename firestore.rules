rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura pública de las convocatorias
    // Permitir escritura (crear, actualizar, eliminar) para gestionar las oportunidades
    match /opportunities/{opportunityId} {
      allow read, write: if true;
    }
  }
}

